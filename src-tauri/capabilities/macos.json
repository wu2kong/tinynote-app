{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "macos-updater",
  "description": "Sparkle updater permissions for macOS",
  "platforms": ["macOS"],
  "windows": ["*"],
  "permissions": [
    "sparkle-updater:default"
  ]
}
